#include<stdio.h>
#include<string.h>
int main()
{
/*	char str[20];
	gets(str);
	int p=strlen(str);
	for(int i=0;i<p;i++)
	{
		if(str[i]!='.')
			printf("%d",(str[i]-48));
	}*/
	double a;
	printf("%d",sizeof(a));
	
//	printf("%d",p);
}
